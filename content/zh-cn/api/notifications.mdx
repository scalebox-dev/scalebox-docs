---
title: 通知 API
description: 用于列出通知并执行已读、未读、删除操作的完整 API 参考。
icon: Bell
---

<Callout type="warn">
**需要身份验证：** 所有通知 API 都需要 `X-API-Key` 请求头。
</Callout>

## 标准响应格式

通知接口使用统一响应包裹：

```json
{
  "success": true,
  "data": {},
  "timestamp": "2026-04-17T12:34:56Z"
}
```

动作类接口现在会同时返回结构化 `data` 与顶层 `message`。

## API 端点

### 列出通知

`GET /v1/notifications`

**查询参数**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | integer | 否 | 返回数量上限，默认 `50`，最大 `100` |
| offset | integer | 否 | 偏移量，默认 `0` |
| read | string | 否 | `true` 仅已读，`false` 仅未读 |
| type | string | 否 | 精确通知类型 |

**响应示例**

```json
{
  "success": true,
  "data": {
    "notifications": [
      {
        "id": "ntf-abc123def456789",
        "user_id": "usr-xyz789abc123456",
        "title": "Sandbox Timeout Warning",
        "message": "Your sandbox will timeout soon",
        "type": "sandbox_timeout",
        "is_read": false,
        "category": "sandbox",
        "created_at": "2026-04-17T11:30:00Z"
      }
    ],
    "total": 1,
    "unread_count": 3,
    "limit": 50,
    "offset": 0
  },
  "timestamp": "2026-04-17T12:00:00Z"
}
```

`unread_count` 表示用户整体未读数量，不受当前筛选条件限制。

### 获取通知

`GET /v1/notifications/{notification_id}`

**响应示例**

```json
{
  "success": true,
  "data": {
    "id": "ntf-abc123def456789",
    "user_id": "usr-xyz789abc123456",
    "title": "Sandbox Timeout Warning",
    "message": "Your sandbox will timeout soon",
    "type": "sandbox_timeout",
    "is_read": false,
    "created_at": "2026-04-17T11:30:00Z",
    "related_entity_type": "sandbox",
    "related_entity_id": "sbx-abc123def456789"
  },
  "timestamp": "2026-04-17T12:01:00Z"
}
```

### 标记通知为已读

`PATCH /v1/notifications/{notification_id}/read`

**响应示例**

```json
{
  "success": true,
  "data": {
    "notification_id": "ntf-abc123def456789",
    "is_read": true
  },
  "message": "Notification marked as read",
  "timestamp": "2026-04-17T12:02:00Z"
}
```

### 标记通知为未读

`PATCH /v1/notifications/{notification_id}/unread`

**响应示例**

```json
{
  "success": true,
  "data": {
    "notification_id": "ntf-abc123def456789",
    "is_read": false
  },
  "message": "Notification marked as unread",
  "timestamp": "2026-04-17T12:03:00Z"
}
```

### 标记全部通知为已读

`PATCH /v1/notifications/read-all`

**响应示例**

```json
{
  "success": true,
  "data": {
    "marked_count": 3,
    "is_read": true
  },
  "message": "All notifications marked as read",
  "timestamp": "2026-04-17T12:04:00Z"
}
```

### 删除单条通知

`DELETE /v1/notifications/{notification_id}`

**响应示例**

```json
{
  "success": true,
  "data": {
    "notification_id": "ntf-abc123def456789",
    "deleted": true
  },
  "message": "Notification deleted",
  "timestamp": "2026-04-17T12:04:30Z"
}
```

### 删除全部通知

`DELETE /v1/notifications`

**响应示例**

```json
{
  "success": true,
  "data": {
    "deleted_count": 4
  },
  "message": "All notifications deleted",
  "timestamp": "2026-04-17T12:05:00Z"
}
```

### 批量删除通知

`DELETE /v1/notifications/bulk-delete`

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| notification_ids | array[string] | 是 | 要删除的通知 ID 列表 |

**响应示例**

```json
{
  "success": true,
  "data": {
    "notification_ids": [
      "ntf-abc123def456789",
      "ntf-def456ghi789012"
    ],
    "deleted_count": 2
  },
  "message": "Notifications deleted",
  "timestamp": "2026-04-17T12:06:00Z"
}
```

### 批量标记为已读

`PATCH /v1/notifications/bulk-read`

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| notification_ids | array[string] | 是 | 要标记为已读的通知 ID 列表 |

**响应示例**

```json
{
  "success": true,
  "data": {
    "notification_ids": [
      "ntf-abc123def456789",
      "ntf-def456ghi789012"
    ],
    "marked_count": 2,
    "is_read": true
  },
  "message": "Notifications marked as read",
  "timestamp": "2026-04-17T12:07:00Z"
}
```

### 批量标记为未读

`PATCH /v1/notifications/bulk-unread`

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| notification_ids | array[string] | 是 | 要标记为未读的通知 ID 列表 |

**响应示例**

```json
{
  "success": true,
  "data": {
    "notification_ids": [
      "ntf-abc123def456789",
      "ntf-def456ghi789012"
    ],
    "marked_count": 2,
    "is_read": false
  },
  "message": "Notifications marked as unread",
  "timestamp": "2026-04-17T12:08:00Z"
}
```

## 常见错误

```json
{
  "success": false,
  "error": "Notification not found",
  "timestamp": "2026-04-17T12:10:00Z"
}
```

- `400`：批量请求体非法，或 `notification_ids` 为空
- `404`：在当前用户范围内找不到通知
- `500`：数据库读取、更新或删除失败
