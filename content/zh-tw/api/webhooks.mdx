---
title: Webhooks API
description: ScaleBox Webhooks 允許您在賬戶中發生事件時接收實時通知。
icon: Webhook
---

ScaleBox Webhooks 允許您在賬戶中發生事件時接收實時通知。無需輪詢 API，Webhook 會自動將事件推送到您的端點，減少 API 呼叫並提供近乎即時的通知。

## 目錄

- [Webhook 概念](#webhook-concepts)
- [API 端點](#api-endpoints)
- [事件型別](#event-types)
- [Webhook 負載](#webhook-payload)
- [安全與驗證](#security)
- [重試機制](#retry-mechanism)
- [最佳實踐](#best-practices)
- [HTTP 狀態碼](#http-status-codes)

<Callout type="warn">
**需要認證：**所有 API 請求都需要使用 `X-API-Key` 請求頭進行認證。
</Callout>

## Webhook 概念

Webhook 是 HTTP 回撥，當 ScaleBox 中發生事件時會通知您的應用程式。無需持續輪詢 API 來檢查狀態變化，Webhook 會自動將事件推送到您的端點。

### Webhook 工作原理

1. **事件發生** - 沙盒啟動、終止或賬戶中發生其他事件
2. **Webhook 檢測** - ScaleBox 檢測到事件並找到匹配的 Webhook 端點（5 秒內）
3. **HTTP POST 交付** - ScaleBox 向您的 Webhook URL 傳送包含事件資料的 HTTP POST 請求
4. **您的端點響應** - 您的端點處理 Webhook 並返回 HTTP 200 OK（失敗時重試）

### Webhook 的優勢

| 優勢 | 描述 |
|------|------|
| **減少 API 呼叫** | 無需輪詢 - 事件自動推送（API 呼叫減少 99%+） |
| **實時通知** | 事件發生後 5 秒內接收通知 |
| **事件驅動架構** | 完美適用於自動化、CI/CD 管道和 AI 代理 |
| **自動重試** | 失敗的交付會自動重試，採用指數退避策略 |

## API 端點

### create-webhook

建立新的 Webhook 端點以接收事件通知

```
POST /v1/webhooks
```

#### 請求引數

| 引數 | 型別 | 必需 | 預設值 | 描述 |
|------|------|------|--------|------|
| `url` | string | 是 | - | 接收 Webhook 的 HTTPS URL |
| `event_types` | array[string] | 是 | - | 要訂閱的事件型別陣列（例如：['sandbox.started', 'sandbox.terminated']） |
| `description` | string | 否 | - | Webhook 端點的可選描述 |

#### 請求示例

```bash
curl -X POST https://api.scalebox.dev/v1/webhooks \
  -H "X-API-Key: sk-1234567890abcdef1234567890abcdef12345678" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-app.com/webhooks/scalebox",
    "event_types": ["sandbox.started", "sandbox.terminated"],
    "description": "生產環境沙盒生命週期事件 Webhook"
  }'
```

#### 響應示例

```json
{
  "success": true,
  "data": {
    "endpoint_id": "whk-abc123def456789",
    "url": "https://your-app.com/webhooks/scalebox",
    "event_types": ["sandbox.started", "sandbox.terminated"],
    "is_active": true,
    "description": "生產環境沙盒生命週期事件 Webhook",
    "timeout_seconds": 30,
    "max_retries": 3,
    "created_at": "2024-01-15T10:30:00Z",
    "updated_at": "2024-01-15T10:30:00Z"
  }
}
```

### list-webhooks

列出您賬戶的所有 Webhook 端點

```
GET /v1/webhooks
```

#### 請求示例

```bash
curl -X GET https://api.scalebox.dev/v1/webhooks \
  -H "X-API-Key: sk-1234567890abcdef1234567890abcdef12345678"
```

#### 響應示例

```json
{
  "success": true,
  "data": {
    "webhooks": [
      {
        "endpoint_id": "whk-abc123def456789",
        "url": "https://your-app.com/webhooks/scalebox",
        "event_types": ["sandbox.started", "sandbox.terminated"],
        "is_active": true,
        "total_deliveries": 150,
        "successful_deliveries": 148,
        "failed_deliveries": 2,
        "last_delivery_at": "2024-01-15T10:25:00Z",
        "last_success_at": "2024-01-15T10:25:00Z",
        "created_at": "2024-01-10T08:00:00Z"
      }
    ]
  }
}
```

### get-webhook

獲取特定 Webhook 端點的詳細資訊

```
GET /v1/webhooks/{endpoint_id}
```

#### 請求示例

```bash
curl -X GET https://api.scalebox.dev/v1/webhooks/whk-abc123def456789 \
  -H "X-API-Key: sk-1234567890abcdef1234567890abcdef12345678"
```

### update-webhook

更新 Webhook 端點配置（URL、事件型別、狀態）

```
PUT /v1/webhooks/{endpoint_id}
```

#### 請求引數

| 引數 | 型別 | 必需 | 預設值 | 描述 |
|------|------|------|--------|------|
| `url` | string | 否 | - | 新的 Webhook 交付 URL |
| `event_types` | array[string] | 否 | - | 更新的訂閱事件型別列表 |
| `is_active` | boolean | 否 | - | 啟用或禁用 Webhook 端點 |
| `description` | string | 否 | - | 更新的描述 |
| `timeout_seconds` | integer | 否 | 30 | HTTP 請求超時時間（5-300 秒） |
| `max_retries` | integer | 否 | 3 | 最大重試次數（0-10） |

#### 請求示例

```bash
curl -X PUT https://api.scalebox.dev/v1/webhooks/whk-abc123def456789 \
  -H "X-API-Key: sk-1234567890abcdef1234567890abcdef12345678" \
  -H "Content-Type: application/json" \
  -d '{
    "is_active": false,
    "event_types": ["sandbox.started", "sandbox.terminated", "sandbox.failed"]
  }'
```

### delete-webhook

刪除 Webhook 端點

```
DELETE /v1/webhooks/{endpoint_id}
```

#### 請求示例

```bash
curl -X DELETE https://api.scalebox.dev/v1/webhooks/whk-abc123def456789 \
  -H "X-API-Key: sk-1234567890abcdef1234567890abcdef12345678"
```

### get-delivery-history

獲取 Webhook 端點的交付歷史和狀態

```
GET /v1/webhooks/{endpoint_id}/deliveries
```

#### 請求引數

| 引數 | 型別 | 必需 | 預設值 | 描述 |
|------|------|------|--------|------|
| `limit` | integer | 否 | 50 | 返回的交付記錄數量（1-200） |

#### 請求示例

```bash
curl -X GET "https://api.scalebox.dev/v1/webhooks/whk-abc123def456789/deliveries?limit=50" \
  -H "X-API-Key: sk-1234567890abcdef1234567890abcdef12345678"
```

#### 響應示例

```json
{
  "success": true,
  "data": {
    "deliveries": [
      {
        "delivery_id": "whd-xyz789abc123456",
        "event_type": "sandbox.started",
        "event_id": "evt-abc123def456789",
        "status": "delivered",
        "http_status_code": 200,
        "attempt_number": 1,
        "max_attempts": 3,
        "delivered_at": "2024-01-15T10:25:00Z",
        "created_at": "2024-01-15T10:25:00Z"
      },
      {
        "delivery_id": "whd-def456ghi789012",
        "event_type": "sandbox.terminated",
        "event_id": "evt-def456ghi789012",
        "status": "retrying",
        "http_status_code": 500,
        "attempt_number": 2,
        "max_attempts": 3,
        "next_retry_at": "2024-01-15T10:27:00Z",
        "created_at": "2024-01-15T10:25:00Z"
      }
    ]
  }
}
```

## 事件型別

ScaleBox 發出各種事件型別，您可以透過 Webhook 訂閱。每個事件型別代表系統中的特定狀態變化或操作。

| 事件型別 | 描述 |
|----------|------|
| `sandbox.created` | 沙盒建立時觸發（尚未執行） |
| `sandbox.started` | 沙盒開始執行並準備就緒時觸發 |
| `sandbox.stopped` | 沙盒停止時觸發（退出執行狀態） |
| `sandbox.paused` | 沙盒暫停時觸發（CPU/記憶體計費停止） |
| `sandbox.resumed` | 沙盒恢復時觸發（CPU/記憶體計費恢復） |
| `sandbox.terminated` | 沙盒完全終止並清理時觸發 |
| `sandbox.failed` | 沙盒啟動失敗或崩潰時觸發 |
| `template.uploaded` | 模板映象上傳到 Harbor 時觸發 |
| `template.deleted` | 模板刪除時觸發 |

## Webhook 負載

所有 Webhook 交付都包含一致的 JSON 負載結構：

```json
{
  "id": "evt-abc123def456789",
  "type": "sandbox.started",
  "timestamp": "2024-01-15T10:30:00Z",
  "data": {
    "sandbox_id": "sbx-xyz789abc123456",
    "status": "running",
    "cpu_count": 2,
    "memory_mb": 4096,
    "storage_gb": 20,
    "template_id": "tpl-python-3.11",
    "project_id": "prj-abc123def456789",
    "owner_user_id": "usr-xyz789abc123456",
    "owner_account_id": "acc-123456789",
    "kubernetes_metadata": {
      "cluster_id": "cls-abc123",
      "namespace_id": "ns-xyz789",
      "pod_name": "sbx-xyz789abc123456",
      "pod_ip": "10.0.0.5",
      "node_name": "worker-node-1"
    }
  }
}
```

### 負載欄位

| 欄位 | 描述 |
|------|------|
| `id` | 唯一事件識別符號 (evt-xxxxx) |
| `type` | 事件型別（例如："sandbox.started"） |
| `timestamp` | 事件發生時的 ISO 8601 時間戳 |
| `data` | 特定於事件的資料物件（因事件型別而異） |

## 安全與驗證

所有 Webhook 交付都包含 HMAC-SHA256 簽名，您可以使用它來驗證請求的真實性。這可以防止篡改並確保 Webhook 來自 ScaleBox。

### HTTP 請求頭

| 請求頭 | 描述 |
|--------|------|
| `X-ScaleBox-Signature` | 負載的 HMAC-SHA256 簽名 |
| `X-ScaleBox-Event-Type` | 事件型別（例如："sandbox.started"） |
| `X-ScaleBox-Event-ID` | 唯一事件識別符號 |
| `Content-Type` | application/json |
| `User-Agent` | ScaleBox-Webhooks/1.0 |

### 簽名驗證示例

#### Python

```python
import hmac
import hashlib

def verify_webhook_signature(payload_body, signature_header, secret):
    """驗證 Webhook 簽名"""
    expected_signature = hmac.new(
        secret.encode('utf-8'),
        payload_body.encode('utf-8'),
        hashlib.sha256
    ).hexdigest()
    
    received_signature = signature_header.replace('sha256=', '')
    return hmac.compare_digest(expected_signature, received_signature)
```

#### Node.js

```javascript
const crypto = require('crypto');

function verifyWebhookSignature(payloadBody, signatureHeader, secret) {
  const expectedSignature = crypto
    .createHmac('sha256', secret)
    .update(payloadBody)
    .digest('hex');
  
  const receivedSignature = signatureHeader.replace('sha256=', '');
  return crypto.timingSafeEqual(
    Buffer.from(expectedSignature),
    Buffer.from(receivedSignature)
  );
}
```

<Callout type="warn">
**重要：金鑰儲存**

Webhook 金鑰在建立端點時生成，永遠不會在 API 響應中暴露。您必須安全地儲存此金鑰以驗證傳入的 Webhook。如果丟失金鑰，您需要刪除並重新建立 Webhook 端點。
</Callout>

## 重試機制

ScaleBox 自動重試失敗的 Webhook 交付以確保可靠性。失敗的交付會使用指數退避策略進行重試。

### 重試行為

| 行為 | 描述 |
|------|------|
| **自動重試** | 失敗的交付（非 2xx HTTP 狀態碼）會自動重試 |
| **指數退避** | 重試間隔：2 分鐘、4 分鐘、6 分鐘（可配置倍數） |
| **最大嘗試次數** | 預設：3 次嘗試（每個端點可配置，0-10） |
| **交付狀態** | `pending` → `retrying` → `delivered` 或 `failed` |

## 最佳實踐

| 最佳實踐 | 描述 |
|----------|------|
| **使用 HTTPS URL** | 始終使用 HTTPS 作為 Webhook 端點以確保安全交付 |
| **驗證簽名** | 始終驗證 X-ScaleBox-Signature 請求頭以確保 Webhook 的真實性 |
| **快速響應** | 在 30 秒內返回 HTTP 200 OK 以避免超時 |
| **冪等處理** | 使用事件 ID 防止在 Webhook 重試時重複處理 |
| **監控交付歷史** | 定期檢查交付歷史以識別和修復端點問題 |

## HTTP 狀態碼

API 返回的標準 HTTP 狀態碼：

| 狀態碼 | 描述 |
|--------|------|
| 200 | 成功 |
| 201 | 已建立 |
| 400 | 錯誤請求 |
| 401 | 未授權 |
| 403 | 禁止訪問 |
| 404 | 未找到 |
| 500 | 伺服器內部錯誤 |
