# API Reference

Complete API reference for ScaleBox, the modern platform for creating and managing isolated development environments. This documentation provides comprehensive information about all available endpoints, authentication methods, and usage examples.

## Getting Started

To use the ScaleBox API, you'll need authentication. All API requests require one of: `X-API-Key` header, `Authorization: Api-Key`, or `Authorization: Bearer`.

### Base Configuration

- **Base URL:** `https://api.scalebox.dev`
- **API Version:** `v1`
- **Authentication:** `X-API-Key` header

## Core Concepts

### RESTful API

All endpoints follow REST conventions with standard HTTP methods (GET, POST, PUT, DELETE) and JSON request/response formats.

### Resource-Based

API resources are organized around sandboxes, projects, templates, and users with clear hierarchical relationships.

### Secure Authentication

All requests require API key authentication with proper scoping and access controls for different user roles.

### Real-time Updates

Webhook support for real-time notifications and status updates as resources change state.

## Available APIs

### Sandbox API

Create, manage, and control sandbox environments.

**Status:** Available

**Documentation:** [Sandbox API](/docs/api/sandboxes)

### Projects API

Organize resources into projects and manage access.

**Status:** Coming Soon

### Templates API

Manage and customize environment templates.

**Status:** Available

**Documentation:** [Templates API](/docs/api/templates)

### Users API

User management and authentication.

**Status:** Coming Soon

## Authentication

All API requests require authentication. You can use any of these methods:

```
X-API-Key: sk-1234567890abcdef1234567890abcdef12345678
Authorization: Api-Key sk-1234567890abcdef1234567890abcdef12345678
Authorization: Bearer your-jwt-token-here
```

## Rate Limiting

API requests are rate-limited to ensure fair usage. Limits are applied per API key and reset periodically.

| Plan | Rate Limit |
|------|------------|
| Standard Plan | 1,000 requests/hour |
| Pro Plan | 10,000 requests/hour |
| Enterprise | Custom limits |

## Error Handling

The API uses standard HTTP status codes and returns detailed error messages in JSON format when requests fail.

| Status Code | Description |
|-------------|-------------|
| 400 | Bad Request (invalid parameters) |
| 401 | Unauthorized (invalid API key) |
| 403 | Forbidden (insufficient permissions) |
| 404 | Not Found (resource doesn't exist) |
| 429 | Too Many Requests (rate limit exceeded) |
| 500 | Internal Server Error |

## Support

Need help with the API? Check out our support resources:

- **API Status:** [status.scalebox.dev](/status)
- **Support:** [support.scalebox.dev](/support)
- **Community:** [community.scalebox.dev](/community)


