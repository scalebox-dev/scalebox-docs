---
title: Profile Commands
description: Reference for viewing and updating your profile and changing your password.
---

Reference for `scalebox-cli profile` commands.

All commands on this page also accept `--compact` for one-line JSON output.

## `profile show`

Show the current user profile.

```bash
scalebox-cli profile show
```

## `profile update`

Update `--display-name` and/or `--description`. The CLI errors if neither flag is set.

```bash
scalebox-cli profile update --display-name "Jane Doe" --description "Platform engineer"
```

## `profile change-password`

Change the current user's password. Pass both `--current-password` and `--new-password`.

```bash
scalebox-cli profile change-password --current-password oldpass --new-password newpass123
```
